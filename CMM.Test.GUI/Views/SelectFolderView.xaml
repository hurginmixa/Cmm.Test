<Window x:Name="SelectedFolderWindow" x:Class="CMM.Test.GUI.Views.SelectFolderView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Folder Selection" Height="190" Width="320"
        xmlns:local="clr-namespace:CMM.Test.GUI.Views"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <Style TargetType="ComboBox">
            <Setter Property="Width" Value="250" />
        </Style>
    </Window.Resources>

    <Grid Name="MainGrid" VerticalAlignment="Top" Height="173">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="6.612"/>
            <ColumnDefinition Width="37.042"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Job:" Margin="5,5,5,5" Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2"/>
        <ComboBox Grid.Row="0" Grid.Column="2" ItemsSource="{Binding Jobs}" SelectedItem="{Binding SelectedJob, Mode=TwoWay}" Margin="5,5,5,5"/>

        <TextBlock Text="Setup:" Margin="5,5,5,5" Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2"/>
        <ComboBox Grid.Row="1"  Grid.Column="2" ItemsSource="{Binding Setups}" SelectedItem="{Binding SelectedSetup, Mode=TwoWay}" Margin="5,5,5,5"/>

        <TextBlock Text="Lot:" Grid.Row="2" Grid.Column="0" Margin="5,5,5,5" Grid.ColumnSpan="2"/>
        <ComboBox Grid.Row="2"  Grid.Column="2" ItemsSource="{Binding Lots}" SelectedItem="{Binding SelectedLot, Mode=TwoWay}" Margin="5,5,5,5"/>

        <TextBlock Text="Wafer:" Grid.Row="3"  Grid.Column="0" Margin="5,5,5,5" Grid.ColumnSpan="2"/>
        <ComboBox Grid.Row="3"  Grid.Column="2" ItemsSource="{Binding Wafers}" SelectedItem="{Binding SelectedWafer, Mode=TwoWay}" Margin="5,5,5,5"/>

        <!-- Кнопки -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5,0,5" Grid.Column="0" Grid.ColumnSpan="3" >
            <Button Content="OK" Width="80" Margin="5" Command="{Binding OkCommand}" IsDefault="True"/>
            <Button Content="Cancel" Width="80" Margin="5" Command="{Binding CancelCommand}" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>